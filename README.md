# GoalModelRepository
This repository organizes and holds a set of reconstructed Goal Models for Goal-Oriented-Requirements Engineering (GORE) in JSON format; reconstructed using the PiStar tool and with their respective metadata. 
All the metadata and other model characteristics are stored in the .xlsx file and the individual models are sorted and stored by model ID in .txt format.
